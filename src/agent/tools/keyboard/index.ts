import {
    typeText,
    getKeyboard,
    typeKey,
    pressKey,
    releaseKey
} from "./tools";

export const keyboardTools = [
    typeText,
    // getKeyboard,
    // typeKey,
    // pressKey,
    // releaseKey
];

export { 
    typeText, 
    getKeyboard,
    typeKey, 
    pressKey, 
    releaseKey 
};
